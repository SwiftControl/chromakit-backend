# ChromaKit Backend Environment Configuration
# Copy this file to .env and update with your actual values

# ========================================
# Supabase Configuration
# ========================================
# Your Supabase project URL
SUPABASE_URL=https://your-project-id.supabase.co

# Supabase anonymous key (public key, safe to expose to clients)
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase service role key (KEEP SECRET! Has admin privileges)
# Used for storage operations to bypass RLS policies
# Leave empty to use SUPABASE_ANON_KEY for storage (respects RLS)
SUPABASE_SERVICE_ROLE_KEY=

# Storage bucket name for uploaded images
SUPABASE_STORAGE_BUCKET=images

# Set to 1 to disable Supabase and use local storage instead
# Useful for development or when running without Supabase
SUPABASE_DISABLED=0

# Directory for local file storage (when SUPABASE_DISABLED=1)
SUPABASE_STORAGE_LOCAL_DIR=.local_storage

# ========================================
# Application Configuration
# ========================================
# Environment: development, staging, production
ENV=production

# Log level: debug, info, warning, error, critical
LOG_LEVEL=info

# Application port (usually managed by Docker)
PORT=8000

